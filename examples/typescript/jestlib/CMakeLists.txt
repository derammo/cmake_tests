# an npm package, package.json is generated from _package_template.json
# requires `include("cmake/derammo_npm.cmake")` at the root
# this section was generated via `make -f ../../cmake/setup.make typescript` (or `npm`)
derammo_npm()
