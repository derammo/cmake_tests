# an npm workspace root, subdirectories containing CMakeLists.txt are added automatically
# requires `include("cmake/derammo_npm.cmake")` at the root
# this section was generated via `make -f ../cmake/setup.make workspaces`
derammo_workspaces_auto()
